version: '3.8'

services:
  ollama-webui:
    build: .
    ports:
      - "8080:8080"
      - "8081:8081"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
